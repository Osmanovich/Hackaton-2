import { register } from "@frukmruk/html";

const VideoInfo = () => {
  return (
    <div
      id="video-info"
      class="w-full h-32 
      p-1 text-gray-800 
      text-sm md:text-lg 
      border border-solid border-gray-600 
      rounded-lg 
      bg-gray-100">
      #Video-info
    </div>
  );
};

register({ VideoInfo });

export default VideoInfo;
