import { register } from "@frukmruk/html";

const Menu = () => {
  return (
    <div
      id="menu"
      class="p-2 hidden 
      md:flex md:w-40 
      min-h-screen
      text-white">
      #Menu
    </div>
  );
};

register({ Menu });

export default Menu;
